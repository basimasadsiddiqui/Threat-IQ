# --- core app ---
fastapi==0.141.1
uvicorn[standard]==0.52.4  # ASGI server (invoked as a CLI, not imported)
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3     # used by pydantic-settings to read .env
httpx==0.28.1
tenacity==9.1.4

# --- agents / llm ---
# These pins are the versions the LangGraph differential test in
# tests/test_langgraph.py was actually run against. The previous pins were a
# 0.x series that was never executed here, so they were shipping untested.
langgraph==1.2.11
langchain-core==1.6.3
langchain-groq==1.1.3
langchain-google-genai==2.1.12   # Gemini provider, not exercised locally
langsmith==0.12.4         # tracing; activated via environment variables

# --- data ---
sqlalchemy==2.0.52
psycopg[binary]==3.2.13   # SQLAlchemy's async PostgreSQL driver
pgvector==0.3.6          # Vector column type for the knowledge store

# --- analysis ---
# Levenshtein distance and cosine similarity are implemented in-tree rather
# than pulled in as dependencies: both are a dozen lines, and the corpus is far
# too small for a native extension to matter.
networkx==3.6.1
pyvis==0.3.2
tldextract==5.3.2
dnspython==2.8.0

# --- ui ---
streamlit==1.63.0
plotly==7.0.0
pandas==3.0.5

# --- observability ---
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-httpx==0.50b0
opentelemetry-instrumentation-sqlalchemy==0.50b0
